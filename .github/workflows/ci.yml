name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruby-ci:
    uses: Captive-Studio/captive-ci/.github/workflows/ruby-ci.yml@ba1fbe95754b07a44da68e3ff59607ced386466b # main
    with:
      lint-command: bundle exec rubocop -f github
