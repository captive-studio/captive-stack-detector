name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruby-ci:
    uses: Captive-Studio/captive-ci/.github/workflows/ruby-ci.yml@a1e4c82696797f8a8b7bc7001c59189c2ecdecf6 # main
    with:
      lint-command: bundle exec rubocop -f github
