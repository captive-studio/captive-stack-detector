name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruby-ci:
    uses: Captive-Studio/captive-ci/.github/workflows/ruby-ci.yml@329c06f8eac5365a98e1193f10b7c07fe25f36ac # main
    with:
      lint-command: bundle exec rubocop -f github
