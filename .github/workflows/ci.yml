name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruby-ci:
    uses: Captive-Studio/captive-ci/.github/workflows/ruby-ci.yml@2362a21908973cdf6d2d001aba5ecdc3af6dc5ab # main
    with:
      lint-command: bundle exec rubocop -f github
