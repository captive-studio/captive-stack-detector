name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruby-ci:
    uses: Captive-Studio/captive-ci/.github/workflows/ruby-ci.yml@bcd9735d584d6d4c9da137cab8de0915cacb525e # main
    with:
      lint-command: bundle exec rubocop -f github
